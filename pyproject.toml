[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["bam_reachability"]

[project]
name = "bam_reachability"
version = "0.0.1"
dependencies = [
  "numpy",
  "open3d",
]
